timeout: 10m

linters-settings:
  gci:
    sections:
      - standard
      - default
      - prefix(github.com/media-streaming-mesh/msm-cni)
  goimports:
    local-prefixes: github.com/media-streaming-mesh/msm-cni

linters:
  disable-all: true
  enable:
    - gofmt
    - gofumpt
    - goimports
    - whitespace

    # FIXME
    # - revive
    # - staticcheck
    # - stylecheck
